const styleAction = (theme) => ({
root: {
    '& .MuiTextField-root': {
      margin: theme.spacing(1),
      width: '25ch',
    },
},
input: {
    marginBottom: theme.spacing(2),
},
});
export default styleAction;




